{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://thruput.io/gettoken/secret-response.schema.json",
  "title": "secret response",
  "description": "What the store answers when it stored a secret: the secret and the version the store gave it, so the caller can ask for that one again later.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "version",
    "value"
  ],
  "properties": {
    "version": {
      "$ref": "defs.schema.json#/$defs/Version"
    },
    "value": {
      "$ref": "defs.schema.json#/$defs/Secret"
    }
  }
}
