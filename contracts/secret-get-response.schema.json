{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://thruput.io/gettoken/secret-response.schema.json",
  "title": "secret response",
  "description": "What the store answers with a secret: the secret and the version it is. Nothing says whether anything was found, because the exit status says that.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "version",
    "value"
  ],
  "properties": {
    "version": {
      "$ref": "defs.schema.json#/$defs/Version"
    },
    "value": {
      "$ref": "defs.schema.json#/$defs/Secret"
    }
  }
}
