name: integration

on:
  pull_request:
  push:
    branches: [main]

jobs:
  chain:
    name: chain runs end to end
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: make test
