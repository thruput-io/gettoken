# 14. Establishing identity

## Context

An agent states the capability it wants. Everything else the request carries is
about the agent rather than from it.

## Decision

The unprivileged side states what it wants. The privileged side establishes `who`,
`doing` and `signed`. They are two schemas.

The privileged side is a component.
