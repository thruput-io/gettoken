{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://thruput.io/gettoken/secret-request.schema.json",
  "title": "secret request",
  "description": "Asking the store for the newest secret under a key. It carries the key alone, and the store answers with the version it holds, so no caller has to know which one that is.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "key"
  ],
  "properties": {
    "key": {
      "$ref": "defs.schema.json#/$defs/Key"
    }
  }
}
