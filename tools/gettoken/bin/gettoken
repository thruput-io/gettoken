#!/bin/sh
# gettoken <scope>  |  gettoken --list
# The agent's entry point. Thin: forwards to the privileged half.
# Scope names and usage live in the man page (man/--help), not a protocol.
exec token-requester "$@"
