# notifier — seat

Concern: summon a human to renew.

Nothing implements it yet. Its place in the request path is in the architecture
diagram in [`README.md`](../../README.md), and the component table there records
where it starts and where it is headed.
